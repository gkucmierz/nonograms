
services:
  piggy-bank:
    container_name: nonograms-app
    build: .
    restart: unless-stopped
    # ports:
    #  - "8081:80"
    expose:
      - "80"
    networks:
      - npm_public

networks:
  npm_public:
    external: true

